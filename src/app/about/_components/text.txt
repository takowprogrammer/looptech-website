import React from 'react';
import { Target, Zap, Eye, ShieldCheck } from 'lucide-react';

export default function AboutValues() {
  const standards = [
    {
      icon: <Target className="w-6 h-6" />,
      title: "Architecture First",
      desc: "We don't troubleshoot symptoms; we solve structural weaknesses. Every solution begins with a blueprint."
    },
    {
      icon: <Zap className="w-6 h-6" />,
      title: "Radical Efficiency",
      desc: "Automation over manual repetition. We deploy Infrastructure as Code (IaC) to ensure speed and zero human error."
    },
    {
      icon: <Eye className="w-6 h-6" />,
      title: "Total Transparency",
      desc: "No black boxes. Our clients have full visibility into their stacks, configurations, and security protocols."
    },
    {
      icon: <ShieldCheck className="w-6 h-6" />,
      title: "Security by Design",
      desc: "Security isn't a layer we add at the end; it is the foundation upon which every byte of data sits."
    }
  ];

  return (
    <section className="py-24 bg-slate-50 border-y border-slate-100">
      <div className="max-w-7xl mx-auto px-6 md:px-8">
        <div className="grid md:grid-cols-2 lg:grid-cols-4 gap-px bg-slate-200 border border-slate-200">
          {standards.map((item, i) => (
            <div 
              key={i} 
              className="bg-white p-10 transition-all duration-300  group"
            >
              <div className="text-primary-600 mb-6  transition-colors">
                {item.icon}
              </div>
              <h4 className="text-xl font-bold text-slate-900 mb-4  transition-colors">
                {item.title}
              </h4>
              <p className="text-slate-500 leading-relaxed text-sm  transition-colors">
                {item.desc}
              </p>
            </div>
          ))}
        </div>
      </div>
    </section>
  );
}